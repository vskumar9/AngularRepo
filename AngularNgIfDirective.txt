The ngIf directive is used to conditionally render elements in the DOM based on a condition.
It evaluates an expression and if the expression is truthy, the element is rendered.
if the expression is falsy, the element is removed from the DOM.
